<core:FragmentDefinition xmlns="sap.m" xmlns:core="sap.ui.core">

	// 1. items="{/}" - use the controller to set and bind the data
	// 2. items="{/WarehouseOrderSrhHelpSet}" - directrly 
    <SelectDialog
		title="Warehouse Order Selection"
		

		items="{/WarehouseOrderSrhHelpSet}"	
		confirm=".onConfirmWarehouseOrder"
        liveChange=".onLiveChangeWarehouseOrder"
		>
		<items>
			<StandardListItem
				title="{WarehouseOrder}" />
		</items>
	</SelectDialog>
</core:FragmentDefinition>